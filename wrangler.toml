name = "ig-feed-preview"
compatibility_date = "2024-11-01"

# Cloudflare Pages — serves the Next.js static export (`output: 'export'` → ./out)
# as a pure static site. URL form: <project>.pages.dev (no account subdomain).
pages_build_output_dir = "out"
